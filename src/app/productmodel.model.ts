export class Productmodel {

    constructor(public name:string,  public detail:string, public price:string, public hero:string, public image:string)
    {
      this.name = name;
      this.detail = detail;
      this.price = price;
      this.hero = hero;
      this.image = image;
      
    }
  }
    
  

